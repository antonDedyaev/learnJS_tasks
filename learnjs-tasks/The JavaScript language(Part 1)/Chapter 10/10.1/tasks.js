// Task 1: Вариант без finally может не сработать в том случае, например, если внутри блока try catch есть return.
// Тогда код после блока никогда не выполнится, а finally гарантирует выполнение кода.